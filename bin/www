require('dotenv').config();
const http = require('http');
const app = require('../server');

const port = parseInt(process.env.DB_PORT, 10) || 3000;
app.set('port', port);

http.createServer(app).listen(port, () => {
    console.log('Server started on port: ' + port)
});
